---
title: 'Scrape Webpage'
openapi: 'POST /upload/scrape_webpage'
---

Scrapes and processes content from a public webpage.

## Sample Request

```bash
curl -X POST https://api.usecortex.ai/upload/scrape_webpage \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "web_url": "https://example.com/article",
    "tenant_id": "tenant_456"
  }'
```

### 🔐 Headers
- `Authorization`: Bearer token

### 🧾 Parameters
- `web_url` (string, required): The public URL to scrape  
- `tenant_id` (string, required): Tenant identifier  
- `sub_tenant_id` (string, optional): Sub-tenant identifier
